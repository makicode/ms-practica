server:
  port: 8081

#spring:
#  cloud:
#    config:
#      server:
#        git:
#          uri: https://github.com/makicode/ms-practica.git
#          searchPath: config-data
#          username: ${GIT_USER}
#          password: ${GIT_PASSWORD}
#          default-label: "main"
#  security:
#    user:
#      name: root
#      password: s3cr3t

spring:
  cloud:
    config:
      server:
        git:
          uri: file:/Users/mac/Documents/Spring/microservices/
          searchPath: config-data
          default-label: "main"
  security:
    user:
      name: root
      password: s3cr3t